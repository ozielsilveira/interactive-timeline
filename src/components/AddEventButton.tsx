import { FC } from 'react';

const AddEventButton: FC = () => {
  const handleClick = () => {
    // Função para abrir modal ou redirecionar para a página de adicionar evento
  };

  return (
    <button onClick={handleClick}>Adicionar Marco</button>
  );
};

export default AddEventButton;
